#include "linkedlist.h"

bool LinkedList::add(&T) {

}
/*
    bool addFirst(T&);
    bool addLast(T&);
    bool add(int, T&);

    void set(int, T&);
    T get(int);

    bool contains(T&);

    T remove(int);
    T removeFirst();
    T removeLast();

    void clear();
    */
